# Claude Code Instructions

Use the shared project instructions in `AGENTS.md`.

Quick reminder:

- This is a transportable beginner website template.
- Keep changes small and verifiable.
- Prefer `src/content` and TinaCMS schema changes before broad component refactors.
- Run `npm run build` after edits.
- Do not edit generated files in `tina/__generated__`, `public/admin`, `.astro`, or `dist`.
